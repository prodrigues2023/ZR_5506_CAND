# Backend

FastAPI service using Clean Architecture, async SQLAlchemy, TVMaze, and a provider-independent AI boundary.

Run tests and linting from this directory with `uv run pytest` and `uv run ruff check app migrations`.
